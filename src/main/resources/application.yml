spring:
  thymeleaf:
    cache: false
  datasource:
    url: jdbc:h2:file:./data/abtest;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
server:
  port: 8080
